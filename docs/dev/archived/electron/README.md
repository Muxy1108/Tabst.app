# Electron-era Archive

This directory preserves migration-era documents that describe the pre-cutover desktop runtime.

## Rules

- Treat everything here as historical context only.
- Do not use these files as implementation guidance for current work.
- For active runtime guidance, use `AGENTS.md` and `docs/dev/TAURI_MIGRATION_STATUS.md`.
