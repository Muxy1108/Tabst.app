# AlphaTex 总览

AlphaTex 是 AlphaTab 的文本谱写格式，通常由三部分组成：

1. 元数据（可选）
2. 乐谱内容（必需）
3. 同步点（可选）

各段之间使用一个点号（`.`）分隔。

- Song metadata：标题、作者、速度等全局信息
- Song contents：轨道/谱表/小节/节拍/音符主体
- Sync points：与外部媒体时间对齐（进阶）

<AlphaTexPlayground sample="overview" />
