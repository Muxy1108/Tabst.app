# Metadata Basics

Metadata follows `\\tag value`, usually at the top of a score. If metadata is present, end metadata with `.` before score content.

Common tags:

- `\title`
- `\artist`
- `\album`
- `\tempo`
- `\instrument`
- `\tuning`

<AlphaTexPlayground sample="metadata-full" />
